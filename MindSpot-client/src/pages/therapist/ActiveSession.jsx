/**
 * ActiveSession — placeholder until real-time session tracking is implemented.
 * Returns null (hidden) until there is a live session to display.
 */
const ActiveSession = () => null;

export default ActiveSession;
